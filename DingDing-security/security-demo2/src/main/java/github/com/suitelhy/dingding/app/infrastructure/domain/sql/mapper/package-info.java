/**
 * 领域层 (Domain) - SQL 的映射实现.
 * -> 该层的映射比较直观, 方便对复杂 SQL 语句进行检查和调整.
 * <p>
 * 该层定义的映射文件应该与 领域层 (Domain) - 领域资源层 (Repository) 接口的名称一致.
 * <p>
 * 目前是只有基于 XML 文件的实现.
 */
package github.com.suitelhy.dingding.app.infrastructure.domain.sql.mapper;